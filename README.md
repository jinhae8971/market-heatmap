# 마켓 히트맵 (개인용)

전세계 금융자산을 트리맵 한 장으로 본다. 면적은 USD 환산 시가총액, 색은 등락률.

## 왜 이 구조인가

- **섹터는 코드에, 시세는 API에.** `yfinance`의 섹터 조회는 종목당 무거운 `.info`
  호출이 필요해 136종목이면 워크플로우가 타임아웃난다. 거의 변하지 않는 섹터는
  `universe.py`에 고정하고, 가격·시총만 `fast_info`로 가볍게 받는다.
- **USD 환산이 필수.** 시총이 로컬통화(KRW·JPY·EUR·CHF·GBp)로 오기 때문에,
  환산하지 않으면 삼성전자가 애플보다 300배 큰 타일이 된다. 런던은 펜스(GBp)
  호가라 100으로 나눈다.
- **종목 단위 실패는 건너뛴다.** 상장폐지·티커 변경 한 건 때문에 히트맵 전체가
  비면 안 된다.

## 구성

```
universe.py    시장 × 섹터 × 종목 (여기만 고치면 종목 추가/삭제)
collect.py     fast_info 병렬 수집 + 환율 환산 → docs/heatmap.json
docs/index.html  D3 트리맵. 모바일 우선, 오프라인 의존성 없음
```

## 유니버스

| 시장 | 종목 수 | 비고 |
|---|---|---|
| 미국 | 43 | 반도체·소프트웨어 비중 높게 구성 |
| 한국 | 38 | KOSPI 중심 + 에코프로비엠(KOSDAQ) |
| 일본 | 25 | |
| 유럽 | 30 | 독·프·영·스위스·네덜란드·이탈리아·스페인 |
| 암호화폐 | 40 | CoinGecko 시총 상위 |

## 사용

- 상단 탭으로 시장 전환. **전체** 탭은 시장별로 묶어 국가 간 비중을 보여준다
- **섹터 묶기** 토글로 그룹핑 해제 가능
- 타일을 누르면 현재가 · 등락률 · USD/현지 시총이 하단 시트에 뜬다

## 실행

```bash
pip install -r requirements.txt
python collect.py
python -m unittest discover -s tests
```

평일 07:00 / 16:30 KST 자동 수집. Pages 소스는 `docs/`.

## 안드로이드 앱

두 가지 방식으로 폰에 설치할 수 있다.

**1) PWA — 설치 없이 즉시**
크롬에서 대시보드를 열고 메뉴 → *홈 화면에 추가*. 아이콘·스플래시·전체화면이
적용되고, 서비스워커가 앱 셸을 캐시해 두 번째 실행부터는 즉시 뜬다.
시세(`heatmap.json`)만 항상 네트워크를 먼저 친다 — 히트맵은 오래된 숫자를
보여주는 게 안 보여주는 것보다 나쁘기 때문이다.

**2) APK — `release/market-heatmap-1.0.apk`**
`android/`에 네이티브 셸이 있다. WebView로 Pages를 띄우는 구조라, 유니버스나
시각화를 고칠 때 APK를 다시 빌드할 필요 없이 레포에 푸시만 하면 다음 실행에
반영된다. 앱이 직접 담당하는 건 오프라인 재시도 UI, 뒤로가기 히스토리 연결,
복귀 시 새로고침 세 가지뿐이다.

```
package  com.jinhae.heatmap
minSdk   24 (Android 7.0)  ·  targetSdk 34
크기     21KB
```

설치: APK를 폰으로 옮긴 뒤 *출처를 알 수 없는 앱 설치* 허용 후 실행.
재빌드: `ANDROID_HOME=<SDK경로> ./android/build.sh`

Gradle을 쓰지 않는다. 액티비티 하나에 외부 의존성이 0개라
`aapt2 → javac → d8 → zipalign → apksigner` 다섯 단계면 끝난다.

## 다음 단계

- 배당수익률 뷰 — `fast_info`에는 없어 별도 소스 필요
- 기간 전환(1주·1개월) — 현재는 전일 대비만. 히스토리 누적이 선행돼야 한다
- 즐겨찾기 탭 — 보유 종목만 모아 보는 개인 유니버스
