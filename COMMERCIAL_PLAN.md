# 상업형 전환 계획

광고·구독으로 수익을 내는 앱으로 올리는 데 필요한 것들. 순서가 중요하다.
아래 1번을 해결하지 않고 2~4번을 먼저 만들면, 만든 것을 대부분 버리게 된다.

---

## 1. 데이터 라이선스 — 유일한 진짜 관문

**지금 구조로는 수익화할 수 없다.** 코드 문제가 아니라 계약 문제다.

- **Yahoo (yfinance)** — 라이브러리 저장소가 명시하듯 이 API는 개인 용도로 의도된
  것이고, yfinance 자체도 연구·교육 목적으로 배포된다. 광고를 붙이거나 구독료를
  받는 순간 개인 용도가 아니게 된다.
- **CoinGecko 무료 티어** — 비상업 용도 전제. 상업 이용은 유료 플랜이 필요하다.

즉 매출이 발생하는 순간 두 소스 모두 교체 대상이다.

### 대응: 공급자 추상화 (구현 완료)

`providers/` 레이어를 넣어 수집기가 특정 공급자에 묶이지 않게 했다.
교체할 때 바꾸는 것은 환경변수 하나다.

```bash
MARKET_DATA_PROVIDER=polygon  POLYGON_API_KEY=...  COMMERCIAL_MODE=1
```

안전장치도 넣었다. `COMMERCIAL_MODE=1`에서 개인용 공급자를 고르면 빌드가
**실패한다**. 실수로 수익화 빌드에 Yahoo가 섞여 들어가는 사고를 코드가 막는다.

```
ProviderLicenseError: 'yahoo'은 개인 용도 라이선스입니다. 수익화 빌드에서는 쓸 수 없습니다.
```

### 공급자 선택 기준

| 항목 | 확인할 것 |
|---|---|
| 커버리지 | 한국·일본·유럽 개별 종목 시가총액까지 주는가 (미국만 주는 곳이 많다) |
| 재배포 권리 | "display to end users" 조항이 있는가. API 접근권과 별개다 |
| 거래소 수수료 | KRX·TSE 등 거래소별 데이터 요금이 별도로 붙는지 |
| 지연 조건 | 15~20분 지연 데이터는 요금이 크게 싸다. 히트맵은 지연으로 충분하다 |
| 출처 표기 | 대부분 화면 표기를 의무화한다 (`provider.attribution`으로 노출 준비됨) |

**지연 데이터로 가는 것이 핵심 절감 포인트다.** 이 앱은 실시간 체결이 필요 없다.
하루 두 번 스냅샷이면 되므로 가장 저렴한 등급으로 협상 가능하다.

---

## 2. Play 정책 — WebView 껍데기는 통과하지 못한다

현재 APK는 GitHub Pages를 띄우는 WebView 셸이다. Play 스팸·최소기능 정책은
**웹사이트의 웹뷰를 제공하는 것이 주 목적인 앱을 허용하지 않는다.** 무료
사이드로드일 때는 문제되지 않았지만, 스토어 정식 등록 + 수익화에서는 걸린다.

### 필요한 전환

| 지금 | 전환 후 |
|---|---|
| 원격 페이지를 WebView로 로드 | 화면을 앱 내부에 내장(로컬 자산 또는 네이티브 렌더) |
| 정적 JSON 파일 | 인증·엔타이틀먼트가 붙은 백엔드 API |
| 없음 | 오프라인 캐시, 푸시 알림, 관심종목 등 네이티브 기능 |

푸시 알림과 관심종목은 정책 통과용 구색이 아니라, 아래 유료 기능의 핵심이기도 하다.

---

## 3. 수익 모델 — 무엇에 돈을 낼 것인가

히트맵 화면 자체는 무료 경쟁자가 많다. **돈을 받을 수 있는 것은 화면이 아니라
알림과 히스토리다.**

### 무료

- 4개 시장 + 암호화폐 히트맵, 하루 2회 갱신
- 섹터 묶기, 종목 상세
- 배너 광고 (히트맵 하단 고정. 전면 광고는 쓰지 않는다 —
  모든 사용자 작업 뒤에 전면 광고를 끼우는 앱은 정책 위반이다)

### 유료 (월 구독)

| 기능 | 왜 돈을 내는가 |
|---|---|
| 관심종목 히트맵 | 내 포트폴리오만 모아 보는 화면. 가장 요청이 많은 기능 |
| 이상 움직임 푸시 | "내 종목이 z-score 2σ 이상 움직임" — 앱을 안 켜도 알 수 있다 |
| 기간 전환 (1주·1개월·3개월) | 오늘 등락만으로는 추세를 못 본다. 히스토리 누적이 자산이 된다 |
| 섹터·국가 로테이션 | Flow Radar에서 이미 만든 자산을 재사용할 수 있다 |
| 광고 제거 | |

**기간 전환은 지금부터 데이터를 쌓아야 나중에 팔 수 있다.** 히스토리는 소급
수집이 안 되므로, 상업화를 확정하기 전이라도 일별 스냅샷 적재는 먼저 시작하는 게 맞다.

---

## 4. 기술 스택 전환

```
[현재]  GitHub Actions → 정적 JSON → Pages → WebView

[전환]  수집 워커 → 시계열 DB → API 서버 (인증·엔타이틀먼트)
                                    ↓
                          앱 (내장 UI + Play Billing + AdMob)
```

- **인증** — 익명 계정 우선. 로그인 강제는 이탈이 크다. 기기 ID 기반으로 시작하고
  기기 이전이 필요할 때만 계정 연결을 유도한다.
- **결제** — Play Billing 구독. **영수증 검증은 반드시 서버에서** 한다.
  클라이언트 검증만 하면 우회가 쉽다.
- **엔타이틀먼트** — 구독 상태를 서버가 판단하고 API 응답에 담아 내려준다.
  앱은 UI만 가린다.
- **광고** — AdMob 배너. 네이티브 셸에 붙이므로 WebView 안이 아니라 바깥에 둔다.

---

## 5. 순서와 비용 감각

| 단계 | 내용 | 선행 조건 |
|---|---|---|
| 0 | 일별 스냅샷 적재 시작 | 없음 — 지금 바로 |
| 1 | 데이터 공급자 계약 | 견적 3곳 비교 |
| 2 | 백엔드 API + 인증 | 1 완료 |
| 3 | 앱 내장 UI 전환 | 2 완료 |
| 4 | Billing + AdMob | 3 완료 |
| 5 | 프로덕션 출시 | 개인 계정은 테스트 요건 충족 필요 |

지속 비용은 **데이터 요금이 대부분**이고 서버비는 부차적이다. 손익분기는
"월 데이터 요금 ÷ 구독 단가"로 단순 계산되므로, 1단계 견적이 나오면
필요한 유료 사용자 수가 바로 나온다.

---

## 솔직한 평가

기술적으로는 전부 만들 수 있다. 다만 **이 앱이 돈을 벌 수 있는지는 기술이 아니라
두 가지에 달려 있다.**

1. 데이터 요금을 감당할 만큼 유료 전환이 나오는가
2. 무료 히트맵 앱(참고한 그 앱 포함)이 이미 있는 시장에서, 관심종목 알림만으로
   차별화가 되는가

1단계 견적을 먼저 받아보시길 권한다. 월 요금 숫자가 나오면 나머지 판단이 쉬워진다.
견적이 예상보다 높으면, 상업화 대신 지금처럼 개인·지인용으로 두는 것도
충분히 합리적인 선택이다.
